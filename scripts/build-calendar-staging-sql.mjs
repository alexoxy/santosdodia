import { createHash } from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';

function argument(name){const index=process.argv.indexOf(name);return index>=0?process.argv[index+1]:undefined}
const inputPath=argument('--input');
const outputPath=argument('--output');
if(!inputPath||!outputPath){console.error('Usage: node scripts/build-calendar-staging-sql.mjs --input <package.json> --output <seed.sql>');process.exit(2)}

const input=JSON.parse(fs.readFileSync(path.resolve(inputPath),'utf8'));
const errors=[];
function text(value,label,{optional=false}={}){if(value===null||value===undefined){if(optional)return null;errors.push(`${label} is required`);return''}if(typeof value!=='string'||!value.trim()){errors.push(`${label} must be a non-empty string`);return''}return value.trim()}
function oneOf(value,allowed,label){if(!allowed.includes(value))errors.push(`${label} must be one of ${allowed.join(', ')}`);return value}
function isoDate(value,label,{optional=false}={}){const normalized=text(value,label,{optional});if(normalized===null)return null;if(!/^\d{4}-\d{2}-\d{2}$/.test(normalized)||Number.isNaN(new Date(`${normalized}T00:00:00Z`).getTime()))errors.push(`${label} must be an ISO date`);return normalized}
function dateTime(value,label){const normalized=text(value,label);if(Number.isNaN(new Date(normalized).getTime()))errors.push(`${label} must be an ISO date-time`);return normalized}
function sha256(value,label,{optional=false}={}){const normalized=text(value,label,{optional});if(normalized===null)return null;if(!/^[a-f0-9]{64}$/.test(normalized))errors.push(`${label} must be a lowercase SHA-256`);return normalized}
function hash(value){return createHash('sha256').update(String(value)).digest('hex')}
function array(value,label){if(!Array.isArray(value)){errors.push(`${label} must be an array`);return[]}return value}
function nullableInteger(value,label,min,max){if(value===null||value===undefined)return null;if(!Number.isInteger(value)||value<min||value>max)errors.push(`${label} must be an integer from ${min} to ${max}`);return value}
function sql(value){if(value===null||value===undefined)return'NULL';if(typeof value==='number')return String(value);return`'${String(value).replaceAll("'","''")}'`}
function bool(value){return value===false?0:1}

if(input?.schemaVersion!=='1.0')errors.push('schemaVersion must equal 1.0');
const run=input?.run??{};
const runId=text(run.id,'run.id');
const runStatus=oneOf(run.status,['provisional','validated','rejected'],'run.status');
const createdAt=dateTime(run.createdAt,'run.createdAt');
const retrievedAt=dateTime(run.retrievedAt,'run.retrievedAt');
const manifestPath=text(run.dropboxManifestPath,'run.dropboxManifestPath');
if(!manifestPath.startsWith('/Santos do Dia/02_Dados_Eclesiasticos/'))errors.push('run.dropboxManifestPath must be inside the approved Dropbox staging root');
const manifestHash=sha256(run.manifestSha256,'run.manifestSha256');
const validationReportPath=text(run.validationReportPath,'run.validationReportPath',{optional:true});
if(validationReportPath&&!validationReportPath.startsWith('/Santos do Dia/02_Dados_Eclesiasticos/'))errors.push('run.validationReportPath must be inside the approved Dropbox staging root');

const sourceAuthority=['official-church','official-jurisdiction','official-local-church','reference-engine','reference-directory','osint-corroboration'];
const policyValidation=['provisional','cross-checked','verified','retired'];
const ruleTypes=['fixed-date','easter-offset','weekday-relative-to-fixed-date','weekday-relative-to-easter','native-calendar-date','annual-source-table','transfer-or-omission'];
const ruleValidation=['provisional','cross-checked','verified','rejected','retired'];
const occurrenceValidation=['provisional','cross-checked','verified','rejected'];
const publicationStatuses=['withheld','publishable'];
const translationStatuses=['source','reviewed','assisted','missing','rejected'];
const sourceIds=new Set(),ruleIds=new Set(),occurrenceIds=new Set();

const sources=array(input.sources,'sources').map((item,index)=>{
 const prefix=`sources[${index}]`,url=text(item?.url,`${prefix}.url`);let host='';try{host=new URL(url).hostname}catch{errors.push(`${prefix}.url must be an absolute URL`)}
 const normalized={id:text(item?.id,`${prefix}.id`),churchId:text(item?.churchId,`${prefix}.churchId`),jurisdictionId:text(item?.jurisdictionId,`${prefix}.jurisdictionId`,{optional:true}),name:text(item?.name,`${prefix}.name`),url,host,authority:oneOf(item?.authority,sourceAuthority,`${prefix}.authority`),usagePolicy:text(item?.usagePolicy,`${prefix}.usagePolicy`),copyrightPolicy:text(item?.copyrightPolicy,`${prefix}.copyrightPolicy`,{optional:true}),active:item?.active!==false};
 if(sourceIds.has(normalized.id))errors.push(`Duplicate source id ${normalized.id}`);sourceIds.add(normalized.id);return normalized
});

const policies=array(input.policies,'policies').map((item,index)=>{const prefix=`policies[${index}]`;const normalized={id:text(item?.id,`${prefix}.id`),churchId:text(item?.churchId,`${prefix}.churchId`),jurisdictionId:text(item?.jurisdictionId,`${prefix}.jurisdictionId`,{optional:true}),engineId:text(item?.engineId,`${prefix}.engineId`),fixedDatePolicy:text(item?.fixedDatePolicy,`${prefix}.fixedDatePolicy`),calendarSystem:text(item?.calendarSystem,`${prefix}.calendarSystem`),effectiveFrom:isoDate(item?.effectiveFrom,`${prefix}.effectiveFrom`,{optional:true}),effectiveTo:isoDate(item?.effectiveTo,`${prefix}.effectiveTo`,{optional:true}),sourceId:text(item?.sourceId,`${prefix}.sourceId`),validationStatus:oneOf(item?.validationStatus,policyValidation,`${prefix}.validationStatus`)};if(!sourceIds.has(normalized.sourceId))errors.push(`${prefix}.sourceId does not exist`);return normalized});

const rules=array(input.rules,'rules').map((item,index)=>{const prefix=`rules[${index}]`;const normalized={id:text(item?.id,`${prefix}.id`),churchId:text(item?.churchId,`${prefix}.churchId`),jurisdictionId:text(item?.jurisdictionId,`${prefix}.jurisdictionId`,{optional:true}),canonicalEventId:text(item?.canonicalEventId,`${prefix}.canonicalEventId`),ruleType:oneOf(item?.ruleType,ruleTypes,`${prefix}.ruleType`),calendarSystem:text(item?.calendarSystem,`${prefix}.calendarSystem`),anchorEventId:text(item?.anchorEventId,`${prefix}.anchorEventId`,{optional:true}),offsetDays:item?.offsetDays??null,month:nullableInteger(item?.month,`${prefix}.month`,1,13),day:nullableInteger(item?.day,`${prefix}.day`,1,31),nativeMonth:text(item?.nativeMonth,`${prefix}.nativeMonth`,{optional:true}),nativeDay:nullableInteger(item?.nativeDay,`${prefix}.nativeDay`,1,30),weekdayRule:text(item?.weekdayRule,`${prefix}.weekdayRule`,{optional:true}),dateRangeStart:isoDate(item?.dateRangeStart,`${prefix}.dateRangeStart`,{optional:true}),dateRangeEnd:isoDate(item?.dateRangeEnd,`${prefix}.dateRangeEnd`,{optional:true}),effectiveFrom:isoDate(item?.effectiveFrom,`${prefix}.effectiveFrom`,{optional:true}),effectiveTo:isoDate(item?.effectiveTo,`${prefix}.effectiveTo`,{optional:true}),sourceId:text(item?.sourceId,`${prefix}.sourceId`),validationStatus:oneOf(item?.validationStatus,ruleValidation,`${prefix}.validationStatus`)};if(normalized.offsetDays!==null&&!Number.isInteger(normalized.offsetDays))errors.push(`${prefix}.offsetDays must be an integer`);if(ruleIds.has(normalized.id))errors.push(`Duplicate rule id ${normalized.id}`);ruleIds.add(normalized.id);if(!sourceIds.has(normalized.sourceId))errors.push(`${prefix}.sourceId does not exist`);return normalized});

const occurrences=array(input.occurrences,'occurrences').map((item,index)=>{const prefix=`occurrences[${index}]`;const normalized={id:text(item?.id,`${prefix}.id`),churchId:text(item?.churchId,`${prefix}.churchId`),jurisdictionId:text(item?.jurisdictionId,`${prefix}.jurisdictionId`,{optional:true}),canonicalEventId:text(item?.canonicalEventId,`${prefix}.canonicalEventId`),dateIso:isoDate(item?.dateIso,`${prefix}.dateIso`),endDateIso:isoDate(item?.endDateIso,`${prefix}.endDateIso`,{optional:true}),nativeCalendarSystem:text(item?.nativeCalendarSystem,`${prefix}.nativeCalendarSystem`,{optional:true}),nativeYear:item?.nativeYear??null,nativeMonth:text(item?.nativeMonth,`${prefix}.nativeMonth`,{optional:true}),nativeDay:item?.nativeDay??null,rankCode:text(item?.rankCode,`${prefix}.rankCode`,{optional:true}),colourCode:text(item?.colourCode,`${prefix}.colourCode`,{optional:true}),ruleId:text(item?.ruleId,`${prefix}.ruleId`,{optional:true}),sourceId:text(item?.sourceId,`${prefix}.sourceId`),sourceRecordUrl:text(item?.sourceRecordUrl,`${prefix}.sourceRecordUrl`,{optional:true}),sourceRecordHash:sha256(item?.sourceRecordHash,`${prefix}.sourceRecordHash`,{optional:true}),validationStatus:oneOf(item?.validationStatus,occurrenceValidation,`${prefix}.validationStatus`),publicationStatus:oneOf(item?.publicationStatus,publicationStatuses,`${prefix}.publicationStatus`)};if(normalized.nativeYear!==null&&!Number.isInteger(normalized.nativeYear))errors.push(`${prefix}.nativeYear must be an integer`);if(normalized.nativeDay!==null&&!Number.isInteger(normalized.nativeDay))errors.push(`${prefix}.nativeDay must be an integer`);if(occurrenceIds.has(normalized.id))errors.push(`Duplicate occurrence id ${normalized.id}`);occurrenceIds.add(normalized.id);if(!sourceIds.has(normalized.sourceId))errors.push(`${prefix}.sourceId does not exist`);if(normalized.ruleId&&!ruleIds.has(normalized.ruleId))errors.push(`${prefix}.ruleId does not exist`);if(normalized.publicationStatus==='publishable'&&!['cross-checked','verified'].includes(normalized.validationStatus))errors.push(`${prefix} cannot be publishable with ${normalized.validationStatus} validation`);return normalized});

const labels=array(input.labels,'labels').map((item,index)=>{const prefix=`labels[${index}]`;const normalized={occurrenceId:text(item?.occurrenceId,`${prefix}.occurrenceId`),locale:text(item?.locale,`${prefix}.locale`),name:text(item?.name,`${prefix}.name`),description:text(item?.description,`${prefix}.description`,{optional:true}),translationStatus:oneOf(item?.translationStatus,translationStatuses,`${prefix}.translationStatus`),sourceLocale:text(item?.sourceLocale,`${prefix}.sourceLocale`,{optional:true})};if(!occurrenceIds.has(normalized.occurrenceId))errors.push(`${prefix}.occurrenceId does not exist`);return normalized});

const observances=new Map();
for(const item of [...rules,...occurrences]){const previous=observances.get(item.canonicalEventId);if(previous&&previous!==item.churchId)errors.push(`Canonical event ${item.canonicalEventId} is assigned to multiple Churches`);observances.set(item.canonicalEventId,item.churchId);if(item.anchorEventId&&!observances.has(item.anchorEventId))observances.set(item.anchorEventId,item.churchId)}
if(runStatus!=='validated'&&occurrences.some(item=>item.publicationStatus==='publishable'))errors.push('Only a validated run may contain publishable occurrences');
for(const occurrence of occurrences.filter(item=>item.publicationStatus==='publishable')){const accepted=labels.filter(label=>label.occurrenceId===occurrence.id&&['source','reviewed'].includes(label.translationStatus));if(!accepted.some(label=>label.locale==='en'))errors.push(`${occurrence.id} needs an accepted English label`);if(!accepted.some(label=>label.locale==='pt'))errors.push(`${occurrence.id} needs an accepted Portuguese label`)}
if(errors.length){console.error(`Calendar staging package rejected with ${errors.length} error(s):`);for(const error of errors)console.error(`- ${error}`);process.exit(1)}

const now=new Date().toISOString();
const statements=['PRAGMA foreign_keys = ON;','BEGIN IMMEDIATE;'];
statements.push(`INSERT INTO calendar_import_runs (id,created_at,retrieved_at,dropbox_manifest_path,manifest_sha256,status,validation_report_path) VALUES (${sql(runId)},${sql(createdAt)},${sql(retrievedAt)},${sql(manifestPath)},${sql(manifestHash)},${sql(runStatus)},${sql(validationReportPath)}) ON CONFLICT(id) DO UPDATE SET retrieved_at=excluded.retrieved_at,dropbox_manifest_path=excluded.dropbox_manifest_path,manifest_sha256=excluded.manifest_sha256,status=excluded.status,validation_report_path=excluded.validation_report_path;`);
for(const item of sources){statements.push(`INSERT INTO source_registry (id,name,base_url,host,authority,adapter,usage_policy,copyright_policy,refresh_hours,requests_per_second,active,updated_at) VALUES (${sql(item.id)},${sql(item.name)},${sql(item.url)},${sql(item.host)},${sql(item.authority)},'calendar-staging',${sql(item.usagePolicy)},${sql(item.copyrightPolicy)},168,0.25,${bool(item.active)},CURRENT_TIMESTAMP) ON CONFLICT(id) DO UPDATE SET name=excluded.name,base_url=excluded.base_url,host=excluded.host,authority=excluded.authority,usage_policy=excluded.usage_policy,copyright_policy=excluded.copyright_policy,active=excluded.active,updated_at=CURRENT_TIMESTAMP;`);statements.push(`INSERT INTO calendar_sources (id,church_id,jurisdiction_id,usage_policy,copyright_policy,active) VALUES (${sql(item.id)},${sql(item.churchId)},${sql(item.jurisdictionId)},${sql(item.usagePolicy)},${sql(item.copyrightPolicy)},${bool(item.active)}) ON CONFLICT(id) DO UPDATE SET church_id=excluded.church_id,jurisdiction_id=excluded.jurisdiction_id,usage_policy=excluded.usage_policy,copyright_policy=excluded.copyright_policy,active=excluded.active;`)}
for(const [eventId,churchId] of observances)statements.push(`INSERT INTO calendar_observances (id,church_id,canonical_key,active,created_at,updated_at) VALUES (${sql(eventId)},${sql(churchId)},${sql(eventId)},1,${sql(now)},${sql(now)}) ON CONFLICT(id) DO UPDATE SET church_id=excluded.church_id,canonical_key=excluded.canonical_key,active=1,updated_at=excluded.updated_at;`);
for(const item of policies)statements.push(`INSERT INTO jurisdiction_calendar_policies (id,church_id,jurisdiction_id,engine_id,fixed_date_policy,calendar_system,effective_from,effective_to,source_id,validation_status) VALUES (${sql(item.id)},${sql(item.churchId)},${sql(item.jurisdictionId)},${sql(item.engineId)},${sql(item.fixedDatePolicy)},${sql(item.calendarSystem)},${sql(item.effectiveFrom)},${sql(item.effectiveTo)},${sql(item.sourceId)},${sql(item.validationStatus)}) ON CONFLICT(id) DO UPDATE SET engine_id=excluded.engine_id,fixed_date_policy=excluded.fixed_date_policy,calendar_system=excluded.calendar_system,effective_from=excluded.effective_from,effective_to=excluded.effective_to,source_id=excluded.source_id,validation_status=excluded.validation_status;`);
for(const item of rules)statements.push(`INSERT INTO calendar_rules (id,church_id,jurisdiction_id,canonical_event_id,rule_type,calendar_system,anchor_event_id,offset_days,month,day,native_month,native_day,weekday_rule,date_range_start,date_range_end,effective_from,effective_to,source_id,validation_status) VALUES (${sql(item.id)},${sql(item.churchId)},${sql(item.jurisdictionId)},${sql(item.canonicalEventId)},${sql(item.ruleType)},${sql(item.calendarSystem)},${sql(item.anchorEventId)},${sql(item.offsetDays)},${sql(item.month)},${sql(item.day)},${sql(item.nativeMonth)},${sql(item.nativeDay)},${sql(item.weekdayRule)},${sql(item.dateRangeStart)},${sql(item.dateRangeEnd)},${sql(item.effectiveFrom)},${sql(item.effectiveTo)},${sql(item.sourceId)},${sql(item.validationStatus)}) ON CONFLICT(id) DO UPDATE SET rule_type=excluded.rule_type,calendar_system=excluded.calendar_system,anchor_event_id=excluded.anchor_event_id,offset_days=excluded.offset_days,month=excluded.month,day=excluded.day,native_month=excluded.native_month,native_day=excluded.native_day,weekday_rule=excluded.weekday_rule,date_range_start=excluded.date_range_start,date_range_end=excluded.date_range_end,effective_from=excluded.effective_from,effective_to=excluded.effective_to,source_id=excluded.source_id,validation_status=excluded.validation_status;`);
for(const item of occurrences){statements.push(`INSERT INTO calendar_occurrences (id,import_run_id,church_id,jurisdiction_id,canonical_event_id,date_iso,end_date_iso,native_calendar_system,native_year,native_month,native_day,rank_code,colour_code,rule_id,validation_status,publication_status,created_at,updated_at) VALUES (${sql(item.id)},${sql(runId)},${sql(item.churchId)},${sql(item.jurisdictionId)},${sql(item.canonicalEventId)},${sql(item.dateIso)},${sql(item.endDateIso)},${sql(item.nativeCalendarSystem)},${sql(item.nativeYear)},${sql(item.nativeMonth)},${sql(item.nativeDay)},${sql(item.rankCode)},${sql(item.colourCode)},${sql(item.ruleId)},${sql(item.validationStatus)},${sql(item.publicationStatus)},${sql(now)},${sql(now)}) ON CONFLICT(id) DO UPDATE SET import_run_id=excluded.import_run_id,date_iso=excluded.date_iso,end_date_iso=excluded.end_date_iso,native_calendar_system=excluded.native_calendar_system,native_year=excluded.native_year,native_month=excluded.native_month,native_day=excluded.native_day,rank_code=excluded.rank_code,colour_code=excluded.colour_code,rule_id=excluded.rule_id,validation_status=excluded.validation_status,publication_status=excluded.publication_status,updated_at=excluded.updated_at;`);const assertionHash=item.sourceRecordHash??hash(`${item.sourceId}|${item.sourceRecordUrl??''}|${item.dateIso}`);const assertionId=`assertion:${item.id}:${item.sourceId}:${assertionHash.slice(0,24)}`;statements.push(`INSERT INTO calendar_occurrence_assertions (id,occurrence_id,source_id,asserted_date_iso,source_record_url,source_record_hash,observed_at,validation_status) VALUES (${sql(assertionId)},${sql(item.id)},${sql(item.sourceId)},${sql(item.dateIso)},${sql(item.sourceRecordUrl)},${sql(assertionHash)},${sql(retrievedAt)},${sql(item.validationStatus)}) ON CONFLICT(id) DO UPDATE SET asserted_date_iso=excluded.asserted_date_iso,source_record_url=excluded.source_record_url,source_record_hash=excluded.source_record_hash,observed_at=excluded.observed_at,validation_status=excluded.validation_status;`)}
for(const item of labels.filter(label=>!['missing','rejected'].includes(label.translationStatus)))statements.push(`INSERT INTO calendar_occurrence_labels (occurrence_id,locale,name,description,translation_status,source_locale) VALUES (${sql(item.occurrenceId)},${sql(item.locale)},${sql(item.name)},${sql(item.description)},${sql(item.translationStatus)},${sql(item.sourceLocale)}) ON CONFLICT(occurrence_id,locale) DO UPDATE SET name=excluded.name,description=excluded.description,translation_status=excluded.translation_status,source_locale=excluded.source_locale;`);
statements.push('COMMIT;','');
fs.mkdirSync(path.dirname(path.resolve(outputPath)),{recursive:true});fs.writeFileSync(path.resolve(outputPath),statements.join('\n'),'utf8');
console.log(`Generated ${outputPath}: ${sources.length} sources, ${observances.size} observances, ${policies.length} policies, ${rules.length} rules, ${occurrences.length} occurrences, ${labels.length} labels.`);
