name: Archived Cloudflare cutover validator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived
        run: echo "Cutover validation completed; this workflow is retained inert until repository cleanup."
