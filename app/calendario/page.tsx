import type { Metadata } from 'next';import CalendarExplorer from '../components/CalendarExplorer';export const metadata:Metadata={title:'Christian Calendar',description:'Browse Catholic and Orthodox saints and feasts by month, tradition, category and region.'};export default function CalendarPage(){return <CalendarExplorer/>}
