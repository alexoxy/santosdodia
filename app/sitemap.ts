import type { MetadataRoute } from 'next';import { getAllObservances } from '../data/observances';export default function sitemap():MetadataRoute.Sitemap{const base='https://santosdodia.com',year=new Date().getUTCFullYear(),staticRoutes=['','/calendar','/explore','/sources','/developers'].map((p,i)=>({url:`${base}${p}`,lastModified:new Date(),changeFrequency:(i===0?'daily':'weekly') as 'daily'|'weekly',priority:i===0?1:.8})),days=getAllObservances(year).map(x=>({url:`${base}/day/${x.dateISO}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.6}));return[...new Map([...staticRoutes,...days].map(x=>[x.url,x])).values()]}
