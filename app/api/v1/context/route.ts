import { NextRequest } from 'next/server';import { localeFromAcceptLanguage } from '../../../../lib/i18n';export async function GET(request:NextRequest){return Response.json({country:request.headers.get('x-vercel-ip-country')??undefined,region:request.headers.get('x-vercel-ip-country-region')??undefined,locale:localeFromAcceptLanguage(request.headers.get('accept-language'))},{headers:{'Cache-Control':'private, max-age=300'}})}
