import { NextRequest } from 'next/server';import { parseCategory,parseTradition,searchObservances } from '../../../../data/observances';import { normalizeLocale } from '../../../../lib/i18n';export async function GET(request:NextRequest){const p=request.nextUrl.searchParams,q=p.get('q')??'',locale=normalizeLocale(p.get('locale')??request.headers.get('accept-language')),year=Number(p.get('year')??new Date().getUTCFullYear()),filters={tradition:parseTradition(p.get('tradition')),category:parseCategory(p.get('category')),country:p.get('country')??undefined},data=searchObservances(q,year,locale,filters).slice(0,100);return Response.json({data,meta:{query:q,locale,year,count:data.length,filters}},{headers:{'Cache-Control':'public, s-maxage=600, stale-while-revalidate=3600','Access-Control-Allow-Origin':'*'}})}
