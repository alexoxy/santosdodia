# Cloudflare preview deployment

Status: experimental preview only.

- Worker: `santosdodia-preview`
- Git branch: `cloudflare-preview`
- Build command: `npm run cloudflare:build`
- Deploy command: `npm run cloudflare:deploy`
- Non-production branch builds: disabled
- Root directory: `/`
- Production domain: unchanged
- Vercel production: unchanged

This file records the controlled first Cloudflare Workers deployment configuration. It does not change application behavior.
